fix(backend): 修复活动日志查不到"创建了笔记"记录

根因：audit.service.ts 写入侧用 new Types.ObjectId() 存成 ObjectId，
查询侧却用字符串数组做 $in 匹配，类型不一致导致匹配不上。

修复：list() 的 $in 数组同时放入 ObjectId 和字符串两种形态，兼容新旧存储。

附带：新增项目专属 project-debug skill（.codebuddy/.agents/.claude 三处同步），
建立 Debug 工作流规范（先查 docs/debug-records.md，确认解决后记录），
并沉淀本次根因与经验教训到 docs/debug-records.md。
