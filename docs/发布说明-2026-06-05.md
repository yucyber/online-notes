# 发布说明（2026-06-05）

## 背景
- 本次联调用于打通前后端启动链路、验证登录流程，并修正文档中的测试账号信息。

## 后端联调改动
- 解除 `KnowledgeBasesModule`、`NotesModule`、`SemanticModule`、`AiModule` 之间的循环导入，确保 Nest 启动阶段模块扫描可正常完成。
- 将 `AggregateSummaryGraph` 和 `KnowledgeGraphBuildGraph` 从 `AiModule` 的 provider 中移除，避免把普通 options 对象误当作可注入依赖。
- 验证结果：后端可以正常启动并监听 `http://localhost:3001`。

## 登录与验证
- 测试账号：`user@example.com`
- 测试密码：`pswd123`
- 前端登录页的自动登录示例已同步更新为 `pswd123`，避免示例与真实账号信息不一致。
- 已确认前端默认 API 地址指向 `http://localhost:3001/api`，登录请求可正常打到后端。

## 风险与说明
- 现有 `password123` 仅保留给其他压力脚本/历史示例场景使用，不再作为该测试账号的登录密码。